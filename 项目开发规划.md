# 万能导入 V2 — 项目开发规划

> 智能多格式批量下单系统：规则引擎 + DeepSeek 辅助规则生成 + Neon + Vercel
>
> 依据《AI 考试题目：万能导入 V2》（2026/06/01 版）编写 · 规划日期 2026-08-04
> 项目目录：`E:\IntelijWorkSpace\AiWorkSpace\import-order`

---

## 一、项目概述

### 1.1 目标

构建一个 Web 应用：用户上传 Excel / Word / PDF 格式的出库单，系统通过**可配置的解析规则体系**（而非硬编码）将文件解析为结构化运单数据，经在线预览编辑、校验后批量下单并持久化到 Neon 数据库，最终部署到 Vercel 在线访问。

核心设计理念：**一套通用规则描述语言（规则引擎）+ AI 自动生成规则草稿 + 用户确认生效**。新增第 5、第 10 种文件格式时系统代码零改动，只需新增一条规则配置。

### 1.2 关键约束（来自考纲）

| 约束 | 说明 |
| --- | --- |
| 技术栈 | Next.js App Router + TypeScript（严格类型） |
| 部署 | Vercel，必须提供可访问 URL（否则 0 分） |
| UI | 鲸天系统风格：主色 `#0fc6c2`、圆角卡片、清爽蓝绿色调 |
| 大模型 | DeepSeek（用户指定），用于**生成解析规则**（不是直接解析数据） |
| 数据库 | Neon PostgreSQL（连接串已提供，走环境变量） |
| 禁止项 | 不得出现文件名判断、特定列名硬编码、针对单文件的 if-else |

### 1.3 评分分布（规划优先级依据）

| 考点 | 分值 | 对应规划章节 | 优先级 |
| --- | --- | --- | --- |
| 1 项目搭建与 Vercel 部署 | 10 | §3.1、阶段 P0/P9 | P0 |
| 2 UI 风格与交互体验 | 30 | §3.6、阶段 P7 | P0 |
| 3 解析规则配置 + AI 辅助生成 | 50 | §3.3/§3.4、阶段 P2/P3 | P0（核心） |
| 4 性能（1000 单 10 秒） | 20 | §3.7、阶段 P8 | P0 |
| 6 V1 基础能力（附加） | 0 | 各模块自然覆盖 | P1 |

---

## 二、样例文件勘察结论（规则引擎设计依据）

`E:\IntelijWorkSpace\AiWorkSpace\demos` 目前实际存在 **6 份**文件，已逐一勘察真实结构；考纲要求 9 份，缺失 3 份（Word 纯文本、周配送计划、多单 PDF），设计上预留对应规则能力，文件到位后仅需补配置规则。

| # | 文件 | 实际结构勘察结论 | 所需规则能力 |
| --- | --- | --- | --- |
| 1 | 黎明屯配送发货单（12.25海口龙湖天街…xlsx） | 42 列；R1 标题、R2-R3 键值元信息（干扰头部）；R4 表头；R5-6 数据；R7 合计行；R8 单据元信息；R9 收货人/电话/地址以「标签-值」横向散落；R10 备注行 | 头部跳过、表格定位、合计行跳过、**尾部标签-值提取** |
| 2 | 湖南仓.xlsx | 32 列；R1 说明行；R2 表头；R3-R169 共 167 行数据；每行自带收货机构(列0)/配送单号(列2)/物品编码(列5)/名称(列6)/规格(列8)/发货数量(列12)/收货人(列26)/电话(列27)/地址(列28) | 头部跳过、**按配送单号跨行聚合**（多物品行共享收货信息） |
| 3 | 欢乐牧场模板0430.xlsx | 19 列；R1 单层表头：列0-12 为 SKU 属性（SKU名称/条码/规格…），**列13-17 为门店名（银泰/金银潭/金桥/门店B/门店D）**，列18 为"下单后结余"；数据 113 行，单元格值=发往该门店的数量 | **SKU×门店矩阵转置**：门店列→独立运单，0/空跳过 |
| 4 | 多门店分Sheet出库单.xlsx | 3 个 Sheet（银泰店/金桥店/金银潭店），结构相同：R1 标题、R2 元信息、R4 表头、R5-11 数据、R12 合计、R14-15 收货门店/联系人/电话/地址标签-值横向、R17 签字行 | **多 Sheet 遍历合并**、每 Sheet 尾部标签-值提取 |
| 5 | 门店调拨单-卡片式.xlsx | R1 标题、R2 元信息；卡片以「▶ 调拨记录 #N」行为边界：卡内含「调入门店/收货人/电话」标签行、「收货地址」标签行、物品小表（表头+数据行）；卡间空行；底部合计行 | **卡片边界识别（行标记正则）**、卡内标签-值 + 小表解析 |
| 6 | 黔寨寨贵州烙锅（鞍山店）常温.pdf | 2 页纯文本流：头部 7 行键值元信息；物品行「序号 类别 编码 名称 规格 单位 数量」（个别行折行）；**表头行出现在第 1 页底部**；"合 计 350"折行合计；尾部「收货人：荣丽 收货电话：13130093946」「收货地址：…」标签-值；页脚「第N页/共M页」「-- N of M --」 | PDF 文本行解析、行正则提取、合计/页脚跳过、**尾部标签-值提取** |
| 7 | 门店配送确认单（Word，缺） | 考纲：纯文本段落、无表格；「━━━」分隔线划分记录；物品行「编号. 编码 \| 名称 \| 规格 \| 数量」 | **纯文本解析**：记录分隔符 + 行内正则/分隔符拆分 |
| 8 | 周配送计划（Excel，缺） | 考纲：日期列头横向（周一~五）、门店纵向、单元格含「物品名x数量\n物品名x数量」复合值 | **双重转置 + 复合单元格拆分** |
| 9 | 配送签收单多单PDF（缺） | 考纲：一个 PDF 内 3 个独立签收单，各有收货人信息 + 物品明细表，分隔线区分 | **PDF 多订单拆分**（记录边界识别）+ 收货信息与明细配对 |

**勘察结论**：9 份文件的差异可归纳为 2 个维度——①记录布局形态（表格 / 矩阵 / 卡片 / 纯文本 / 复合单元格）；②辅助信息位置（列内随行走 / 尾部标签-值 / 卡片内 / 记录块内）。规则引擎 DSL 按这两个维度设计即可全覆盖（见 §3.3）。

---

## 三、总体架构设计

### 3.1 技术选型

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 框架 | Next.js 14 (App Router) + TypeScript | 考纲要求；RSC + Route Handlers 一体 |
| 样式 | Tailwind CSS + CSS 变量主题 | 快速还原鲸天风格（主色 `#0fc6c2`、圆角 12px、卡片阴影） |
| Excel 解析 | SheetJS（xlsx）前端解析 | 纯前端、快、支持合并单元格与多 Sheet；1000 行解析远低于 10s |
| Word 解析 | 前端解包 docx（jszip）抽取 `word/document.xml` 文本段落 | docx 本质 zip+xml，避免引入重型库 |
| PDF 解析 | pdfjs-dist 前端提取文本项（含坐标） | 可按 y 坐标重组行，处理折行/多栏 |
| 规则执行 | 自研规则引擎，**浏览器端执行**（纯函数、与 React 无关） | 避免 Vercel Serverless 10s 超时；大文件不占服务端 |
| 大模型 | DeepSeek `deepseek-chat`，OpenAI 兼容协议 `https://api.deepseek.com/chat/completions` | 用户指定；仅服务端 Route Handler 调用，Key 不出前端 |
| 数据库 | Neon PostgreSQL + `@neondatabase/serverless`（HTTP fetch 模式） | Vercel 友好、无需长连接；连接串走环境变量 |
| 虚拟列表 | @tanstack/react-virtual | 1000+ 行预览不卡顿（考点 4） |
| Excel 导出 | SheetJS 前端导出 .xlsx | 预览数据（含修改）导出 |
| 校验提示 | sonner（Toast）+ 自研校验引擎 | 鲸天风格 Toast |

### 3.2 数据流总览

```
上传文件 ──► 前端归一化 IR（NormalizedDocument）
                │  excel→单元格矩阵(含合并/Sheet)  word→段落文本  pdf→坐标文本行
                ▼
        用户选择已有规则 ──► 规则引擎执行 ──► 结构化运单草稿
        或「新建规则」
                │
                ▼ 新建规则分支
        IR 摘要（前N行样例）──► POST /api/rules/ai-generate ──► DeepSeek
                │                                                    │
                │            ◄── 规则 JSON + 逐字段置信标注（推测/确认）
                ▼
        规则编辑器（表单+JSON 双视图）── 用户微调 ──► 预览试解析（前端即时执行）
                │                                        │ 通过
                ▼                                        ▼
        解析结果进入预览编辑页 ◄───────────────────────┘
                │
                ▼
        行内校验 + 外部编码查重（同批次 + POST /api/waybills/check-dup）
                │
                ▼
        提交下单 ──► POST /api/waybills ──► 事务写入 Neon ──► 结果汇总
```

要点：**文件解析与规则执行全部在浏览器完成**（性能考点 + 规避 Serverless 超时）；服务端只负责规则 CRUD、AI 调用、查重、下单持久化。

### 3.3 规则引擎 DSL 设计（核心考点，50 分）

规则为一份 JSON（TypeScript 类型约束 + zod 运行时校验），存入 Neon `parse_rules.spec`（jsonb）。

```ts
// ===== 顶层 =====
interface ParseRule {
  name: string;
  description?: string;
  fileType: 'excel' | 'word' | 'pdf' | 'auto';
  spec: RuleSpec;
  // AI 生成时附带：每个映射的来源说明与置信度，供 UI 标注「推测」徽标
  fieldMeta?: Record<string, { inferred: boolean; note: string }>;
}

interface RuleSpec {
  // ① Sheet 选择（Excel 专用）
  sheet?: { mode: 'all' | 'first' | 'byIndex'; indexes?: number[] };

  // ② 记录布局（五选一，对应五种形态）
  layout:
    | TableLayout      // 标准表格（黎明屯/湖南仓/多门店Sheet/黔寨寨PDF）
    | MatrixLayout     // 矩阵转置（欢乐牧场/周配送计划）
    | CardLayout       // 卡片式（门店调拨单）
    | TextLayout       // 纯文本段落（Word 门店配送确认单）
    | MultiDocLayout;  // 单文件多订单（多单 PDF：先拆记录块，块内再套 Table/Text）

  // ③ 字段映射（目标字段 ← 来源）
  fields: FieldMapping[];

  // ④ 聚合：同一外部编码/分组键的多行合并为一个运单
  groupBy?: { by: FieldRef; };

  // ⑤ 全局过滤
  skip?: SkipRule[];   // 如：行内容包含"合计"则跳过；匹配页脚正则则跳过
}

// ===== 布局定义 =====
interface TableLayout {
  type: 'table';
  headerLocate: { rowIndex?: number; matchText?: string }; // 表头定位（行号或文本匹配，黔寨寨表头在页底也能定位）
  dataRange: { start: 'afterHeader' | number; end?: number | { untilText: string } };
  itemFieldsInRow: boolean;      // 物品字段是否随行（是→列映射；否→配合 fields 的其他来源）
}
interface MatrixLayout {
  type: 'matrix';
  rowEntity: { headerRowIndex: number; labelCols: number[] };      // 行方向实体（SKU 属性列）
  colEntity: { headerRowIndex: number; colRange: [number, number] }; // 列方向实体（门店/日期）
  value: { skipEmptyOrZero: boolean };
  expandTo: 'records';           // 每个(行实体×列实体)非零单元格 → 一条物品行
  cellSplit?: { delimiter: string; itemPattern: string }; // 复合单元格拆分（周配送计划）
}
interface CardLayout {
  type: 'card';
  marker: { matchText: string; regex?: string };  // 卡片起始标志，如 "▶ 调拨记录"
  inner: { labels: LabelValueLocate[]; table: TableLayout };
}
interface TextLayout {
  type: 'text';
  recordSeparator?: { regex: string };     // 记录分隔线，如 ━━━
  itemLine: { regex: string; groups: Record<string, number> }; // 物品行正则捕获组
}
interface MultiDocLayout {
  type: 'multiDoc';
  splitBy: { regex: string } | { pageBreak: true };
  inner: TableLayout | TextLayout;
}

// ===== 字段来源（多对一、静态、默认值都靠它组合） =====
type FieldSource =
  | { kind: 'column'; index?: number; headerMatch?: string }   // 表格列（按序号或按表头文本匹配，禁止写死列名）
  | { kind: 'labelValue'; label: string; scope: 'row' | 'card' | 'sheet' | 'doc'; regex?: string } // 标签-值
  | { kind: 'regex'; pattern: string; group: number; scope: 'line' | 'doc' }
  | { kind: 'static'; value: string }                          // 静态值
  | { kind: 'sheetName' }                                      // Sheet 名
  | { kind: 'split'; from: FieldRef; delimiter: string; index: number }; // 复合单元格拆分取值

interface FieldMapping {
  target: 'externalCode' | 'store' | 'receiverName' | 'receiverPhone'
        | 'receiverAddress' | 'skuCode' | 'skuName' | 'skuQty'
        | 'skuSpec' | 'remark';
  source: FieldSource;
  default?: string;        // 默认值
  transform?: 'trim' | 'toNumber' | 'phoneNormalize' | { regex: string; replace: string };
}

interface SkipRule { when: { cellContains?: string; lineMatches?: string }; scope: 'row' | 'line'; }
```

**能力 × 文件覆盖矩阵**（验收依据，打勾=该规则能力被某文件强制需要）：

| 能力 | 黎明屯 | 湖南仓 | 欢乐牧场 | 黔寨寨PDF | 多Sheet | 卡片式 | Word文本 | 周计划 | 多单PDF |
| --- | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: |
| 头部跳过 skip/headerLocate | ● | ● | | ● | ● | ● | | | ● |
| 尾部标签-值 labelValue(scope) | ● | | | ● | ● | ● | ● | | ● |
| 跨行聚合 groupBy | | ● | | | | | ● | | |
| 矩阵转置 MatrixLayout | | | ● | | | | | ● | |
| 复合单元格拆分 cellSplit | | | | | | | | ● | |
| 多 Sheet 合并 sheet.mode=all | | | | | ● | | | | |
| 卡片边界 CardLayout.marker | | | | | | ● | | | |
| 记录分隔 TextLayout.separator | | | | | | | ● | | ● |
| 行正则提取 itemLine.regex | | | | ● | | | ● | | ● |
| 合计行/页脚跳过 skip | ● | | | ● | ● | ● | | | |
| 静态值/默认值/Sheet名取值 | | | | | ● | | | | |

### 3.4 AI 辅助规则生成（DeepSeek）

职责边界：**AI 生成规则 JSON，不直接解析数据**（考纲考点 3 明确要求）。

调用链：`POST /api/rules/ai-generate`（Route Handler，仅服务端持有 Key）

- 输入 = IR 摘要（控制 token）：Sheet 清单与行列规模、表头候选行（前 15 行非空单元格带坐标）、数据区抽样 3 行、尾部 10 行、文本文件的段落抽样；附目标字段说明与 DSL JSON Schema。
- Prompt 设计（封装于 `lib/ai/prompt.ts`，独立可替换）：
  - System：角色=解析规则配置专家；只输出符合 Schema 的 JSON；对每个 `fields[]` 映射在 `fieldMeta` 中标注 `inferred: true/false` 与理由；不确定时给候选并说明。
  - User：IR 摘要 + 输出要求（含「禁止输出任何解释性文字，只输出 JSON」）。
  - 参数：`model: deepseek-chat`、`temperature: 0.2`、`max_tokens: 4096`；超时控制 60s（AbortController），失败返回结构化错误。
- 输出处理：zod 校验 → 不合法则带错误信息重试 1 次 → 仍失败提示用户手动配置。
- UI 侧：AI 规则进入编辑器后，推测字段显示橙色「推测」徽标与说明，用户逐项确认/微调 → 「试解析」通过后保存。

安全与纪律要求：API Key 仅存环境变量（本地 `.env.local`、线上 Vercel Environment Variables），**不入库、不进 Git、不暴露给前端**；Prompt 为本项目原创设计（考纲反作弊要求：不得与他人 Prompt 雷同）。

### 3.5 数据库设计（Neon PostgreSQL）

```sql
-- 解析规则
CREATE TABLE parse_rules (
  id          SERIAL PRIMARY KEY,
  name        TEXT NOT NULL,
  description TEXT,
  file_type   TEXT NOT NULL,              -- excel | word | pdf | auto
  spec        JSONB NOT NULL,             -- RuleSpec 全文
  field_meta  JSONB,                      -- AI 置信标注
  source      TEXT NOT NULL DEFAULT 'manual',  -- manual | ai
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 导入批次（一次提交 = 一个批次）
CREATE TABLE import_batches (
  id          SERIAL PRIMARY KEY,
  file_name   TEXT,
  rule_id     INT REFERENCES parse_rules(id),
  total_rows  INT, success_rows INT, failed_rows INT,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 运单（按外部编码聚合后的出库单）
CREATE TABLE waybills (
  id              SERIAL PRIMARY KEY,
  batch_id        INT REFERENCES import_batches(id),
  external_code   TEXT,                    -- 外部编码（可空）
  receiver_mode   TEXT NOT NULL,           -- A=门店 | B=收件人
  store_name      TEXT, receiver_name TEXT,
  receiver_phone  TEXT, receiver_address TEXT,
  remark          TEXT,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 运单物品行
CREATE TABLE waybill_items (
  id          SERIAL PRIMARY KEY,
  waybill_id  INT REFERENCES waybills(id) ON DELETE CASCADE,
  sku_code    TEXT NOT NULL, sku_name TEXT NOT NULL,
  qty         NUMERIC(12,2) NOT NULL,
  spec        TEXT, remark TEXT
);
CREATE INDEX idx_waybills_ext ON waybills(external_code);
CREATE INDEX idx_waybills_created ON waybills(created_at DESC);
```

外部编码查重：提交前 `POST /api/waybills/check-dup` 传入本批次编码数组，服务端一次 `SELECT external_code FROM waybills WHERE external_code = ANY($1)` 返回已存在集合，前端据此标红并注明「与已入库数据重复」。

### 3.6 页面与交互设计（考点 2，30 分）

鲸天风格设计语言落地：主色 `#0fc6c2`（按钮/高亮/链接），浅青底 `#e8fafa` 标签，卡片圆角 12px、柔和阴影 `0 2px 12px rgba(0,0,0,.06)`，正文 `#1d2129/#4e5969` 两级灰，间距 8 的倍数。

| 路由 | 页面 | 关键交互 |
| --- | --- | --- |
| `/` | 文件导入（首页） | 拖拽/点击上传卡片；规则选择列表 + 醒目「新建规则」按钮；解析实时进度条（百分比 + 已处理/总条数）；失败时展示文件原始信息并给出手动配置入口 |
| `/preview` | 预览编辑 | 虚拟滚动表格；表头固定 + 横向滚动；单元格点击编辑（Enter/Tab 切换）；错误单元格红框 + 悬浮原因；顶部错误汇总面板（一次性列出全部：行号/字段/原因）；重复行高亮并注明与哪行重复；删除行/新增空行；导出 Excel |
| `/rules` | 规则管理 | 列表（名称/适用类型/来源 AI手动/更新时间）；新建、编辑、复制、删除（二次确认）；编辑器=表单视图 + JSON 视图切换 |
| `/waybills` | 已导入运单 | 筛选（外部编码/收件人姓名/提交时间区间）+ 分页；点击展开物品明细 |

交互细节清单（逐项实现并自测）：所有异步按钮 Loading + 防重复点击；成功/失败 Toast；空状态占位插画；路由切换与弹窗过渡动画；≥1280px 双栏、<768px 单栏，表格横向滚动，关键操作按钮吸底可见。

校验规则（预览页实时执行，全部错误一次性展示）：SKU 编码/名称/数量必填；数量必须为正数；电话格式 `1[3-9]\d{9}`（或座机）；A/B 组二选一必填（A=门店，B=姓名+电话+地址）；外部编码同批次内重复、与数据库重复均标红注明。

### 3.7 性能方案（考点 4，20 分）

| 指标 | 方案 |
| --- | --- |
| 1000 单上传→预览 ≤10s（不含 AI） | 解析与规则执行全部在前端（Web Worker 中跑 SheetJS + 规则引擎，不阻塞 UI）；服务端只接收最终结构化结果，单次批量 INSERT |
| 前端渲染 1000 条 ≤3s | @tanstack/react-virtual 虚拟列表（可视区 ~30 行），单元格编辑态按需挂载 |
| 大文件不阻塞 UI | IR 归一化与规则执行放入 Web Worker，主线程只更新进度条（postMessage 分片上报进度） |
| 内存 | SheetJS `{ dense: true }` 读取；IR 只保留非空单元格；预览数据用扁平数组不做深拷贝；提交后释放 Worker 与文件 ArrayBuffer |

### 3.8 目录结构

```
import-order/
├── app/
│   ├── layout.tsx                 # 全局布局 + 鲸天风格主题
│   ├── page.tsx                   # 首页：上传与规则选择
│   ├── preview/page.tsx           # 预览编辑
│   ├── rules/page.tsx             # 规则管理
│   ├── waybills/page.tsx          # 已导入运单
│   └── api/
│       ├── rules/route.ts         # GET 列表 / POST 新建
│       ├── rules/[id]/route.ts    # PUT 更新 / DELETE 删除 / POST 复制
│       ├── rules/ai-generate/route.ts   # DeepSeek 生成规则
│       ├── waybills/route.ts      # POST 提交下单 / GET 列表查询
│       └── waybills/check-dup/route.ts  # 外部编码查重
├── components/
│   ├── upload/DropZone.tsx, ProgressPanel.tsx
│   ├── rules/RuleEditor.tsx, FieldMappingForm.tsx, RuleJsonView.tsx
│   ├── preview/VirtualTable.tsx, EditableCell.tsx, ErrorSummary.tsx
│   └── ui/ (Button, Card, Modal, Toast, Pagination ...)
├── lib/
│   ├── ir/excel.ts, word.ts, pdf.ts, types.ts    # 归一化 IR
│   ├── engine/types.ts, executor.ts, sources.ts  # 规则引擎（纯函数）
│   ├── ai/deepseek.ts, prompt.ts, schema.ts      # AI 封装（超时/重试/zod）
│   ├── db/index.ts, rules.ts, waybills.ts        # Neon 访问层
│   └── validate.ts                               # 校验引擎
├── worker/parse.worker.ts         # Web Worker：IR + 规则执行 + 进度上报
├── types/index.ts
├── .env.local                     # DEEPSEEK_API_KEY / DATABASE_URL（不入库）
└── README.md                      # 含大模型调用说明（提交要求 3）
```

---

## 四、实施计划（里程碑与排期）

以 AI 辅助开发为基准估算；每项含完成标准（DoD）。

| 阶段 | 内容 | 产出 / 完成标准 | 预估 |
| --- | --- | --- | --- |
| **P0 脚手架与先行部署** | create-next-app(TS) + Tailwind 主题变量；git init；Neon 连接测试脚本；**先部署空壳到 Vercel** | Vercel 有 URL 且页面可访问；`git push` 自动部署链路跑通 | 0.5h |
| **P1 数据层** | 建表 SQL（§3.5）；规则 CRUD API；运单写入/查询/查重 API；zod 入参校验 | curl/Postman 全接口通过；错误返回结构统一 | 1h |
| **P2 规则引擎（核心）** | IR 归一化（Excel/Word/PDF）；DSL 类型与执行器；用现有 6 份样例逐一手写规则 JSON 验证 | 6 份样例全部解析出正确运单（黎明屯 2 行物品+尾部收货、湖南仓按单号聚合、欢乐牧场矩阵转置、多 Sheet 3 店、卡片式 3 卡、黔寨寨 41 行物品+尾部收货） | 3h |
| **P3 AI 规则生成** | DeepSeek 客户端封装（超时/重试/zod）；Prompt 与 IR 摘要构造；规则编辑器（表单+JSON+推测徽标+试解析） | 对任一样例一键生成规则草稿，微调后试解析通过并可保存 | 1.5h |
| **P4 导入流程 UI** | 上传（拖拽/点击）、规则选择/新建入口、进度条、错误处理与失败兜底（展示原始信息+手动配置入口） | 完整走通 上传→选规则→解析→结果 | 1.5h |
| **P5 预览编辑** | 虚拟表格、单元格编辑、校验引擎、错误汇总面板、重复检测、增删行、导出 Excel | 1000 行渲染 ≤3s；全部错误一次列出；导出文件与页面一致 | 2h |
| **P6 提交与运单列表** | 提交进度条、事务写库、结果汇总；运单列表筛选/分页 | 有错行禁止提交；成功 N/失败 N 汇总正确；历史可查 | 1h |
| **P7 UI 打磨** | 鲸天风格走查、Toast/Loading/空状态/动画、响应式 | 对照考点 2 细则逐项自测 | 1h |
| **P8 性能验收** | 构造 1000 行标准 Excel 计时；Worker 化检查；内存观察 | 上传→预览 ≤10s；无卡顿无溢出 | 1h |
| **P9 部署验收与提交材料** | 生产构建零错误；Vercel 环境变量配置；README（模型/Prompt 思路/Key 配置方式）；Git 仓库整理 | 提交三件套：Vercel URL + 仓库链接 + 大模型调用说明 | 0.5h |

合计约 **13h（AI 辅助连续工时）**；若按考纲 120 分钟限时执行，策略为：P0→P2→P3→P4→P5→P6 串行保核心分（考点 3 的 50 分 + 兼容矩阵），P7/P8 在收尾并行打磨。**先部署后开发**：P0 结束即有在线 URL，确保考点 1 的 10 分先落袋。

## 五、风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| DeepSeek 输出 JSON 不稳定 | AI 规则不可用 | temperature 0.2；Schema 强约束 + zod 校验 + 1 次自动重试；失败降级为手动配置（入口常在） |
| AI 调用慢（>10s） | 用户等待体验差 | 前端转圈+分步文案；服务端 60s 超时熔断；IR 摘要压缩 token；AI 时间不计入性能指标（考纲豁免） |
| PDF 文本提取折行/乱序 | 解析错行 | pdfjs 按 y 坐标聚类重组行；行正则允许跨行拼接（`件 件 1` 场景）；兜底把文本样例交 AI 生成行正则 |
| Vercel Serverless 超时 | 下单接口失败 | 批量 INSERT 单请求完成（1000 行约 1 次多值 INSERT），无循环单条插入 |
| 缺失的 3 份样例文件 | 兼容矩阵无法全量验收 | DSL 已含 TextLayout/cellSplit/MultiDocLayout；文件到位后只补规则配置不改代码（恰是考点） |
| 反作弊（Prompt 雷同） | 成绩 0 分 | Prompt 原创编写并记录设计思路入 README；UI 以鲸天风格自行设计不用现成模板 |
| Key 泄露 | 安全事故 | 仅环境变量；`.env.local` 入 `.gitignore`；Vercel 端用 Environment Variables 而非硬编码 |

## 六、验收清单（对照评分标准逐项自查）

**考点 1（10 分）**：☐ Vercel URL 可访问 ☐ 构建零错误 ☐ Next.js App Router + TS

**考点 2（30 分）**：☐ 主色 #0fc6c2 贯穿 ☐ 卡片/圆角/阴影统一 ☐ Loading/Toast/防重复/空状态/动画 ☐ 响应式与表格横滚

**考点 3（50 分）**：☐ 规则 JSON 持久化、可增删改复制 ☐ 手动选择规则，无自动匹配 ☐ AI 生成规则带推测标注、可编辑预览确认后生效 ☐ 9 份文件兼容矩阵（每份文件一条独立规则配置） ☐ 代码全文搜索无文件名判断、无特定列名硬编码 ☐ 覆盖头部跳过/尾部提取/跨行聚合/矩阵转置/多对一/默认值/静态值

**考点 4（20 分）**：☐ 1000 单 ≤10s（附计时截图） ☐ 渲染 ≤3s ☐ 虚拟列表 ☐ 大文件无阻塞无溢出

**V1 基础（附加）**：☐ 校验全量一次性展示（行号/字段/原因） ☐ 类 Excel 编辑（Tab/回车切换） ☐ 导出 xlsx ☐ 外部编码双向查重并注明重复来源 ☐ 数据入库非本地存储 ☐ 运单列表筛选+分页

## 七、提交物清单

1. Vercel 在线访问 URL（P0 阶段先行产出）
2. Git 仓库（GitHub/Gitee），提交历史体现开发过程
3. README 大模型调用说明：模型 `deepseek-chat`（DeepSeek）、Prompt 设计思路（角色约束 + Schema 约束 + 置信标注 + IR 摘要输入）、Key 配置方式（`.env.local` / Vercel Environment Variables，不入库）
4. 本规划文档与样例文件兼容矩阵（置于仓库 `docs/`）

---

*附：环境变量清单（值已获取，配置于 `.env.local` 与 Vercel，不写入文档与代码库）*

```
DEEPSEEK_API_KEY=<DeepSeek API Key>
DEEPSEEK_BASE_URL=https://api.deepseek.com
DATABASE_URL=<Neon pooler 连接串，含 channel_binding=require&sslmode=require>
```
