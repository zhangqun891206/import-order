# 提交物清单（V4 专项考核 ）


| # | 提交物 | 交付内容 / 位置 | 状态 |
|---|---|---|---|
| 1 | 在线地址（Vercel 可访问 URL） | https://import-order-one.vercel.app/ | ✅ |
| 2 | 源码仓库 | git@github.com:zhangqun891206/import-order.git<br>（HTTPS：https://github.com/zhangqun891206/import-order） | ✅ |
| 3 | 压测数据脚本（20,000 条 SKU 主数据） | `scripts/seed-data.mjs`：清理+插入 20,000 条 SKU（SKU_00001~20000）、生成压测 Excel、创建压测规则；可重复执行 | ✅ |
| 4 | 10,000 行压测 Excel 文件 | `test-data/10000-orders.xlsx`（10,000 行，含 ~1% 注入非法行） | ✅ |
| 5 | 压测报告（证明 10,000 行 ≤60s） | `docs/压测报告.md` + 压测脚本 `scripts/loadtest.mjs`（输出 meet_60s 判定） | ✅ |
| 6 | 架构设计文档（异步流程图/Outbox/批量策略） | `docs/架构设计-V4.md`（含重构前后 mermaid 流程图、Outbox、批量校验/写入、幂等、状态机、事件契约） | ✅ |
| 7 | 《重构假设说明》（覆盖模块十一） | `docs/重构假设说明.md`（12 项要求逐条覆盖） | ✅ |
| 8 | 接口文档（上传/任务/错误/Trace/监控聚合） | `docs/接口文档.md`（9 个 V4 接口 + V2 复用接口，含请求/响应示例） | ✅ |
| 9 | README（本地启动/环境变量/部署/压测/故障模拟） | `README.md`（快速开始、环境变量、Vercel 部署、压测脚本、故障模拟小节） | ✅ |
| 10 | 演示账号或访问说明 | 无需登录，直接访问；页面见下表 | ✅ |

## 演示访问说明（第 10 项）
线上 https://import-order-one.vercel.app/ 无需账号，可直接查看：

| 页面 | 路径 |
|---|---|
| 异步导入（上传→任务） | `/` |
| 导入任务列表 / 详情 | `/tasks`、`/tasks/[id]` |
| 监控看板 | `/monitor` |
| Trace 检索 | `/traces` |
| 解析规则 | `/rules` |
| 已导入运单 | `/waybills` |
| 旧版同步导入 | `/legacy` |

## 补充交付物（超出清单要求）
- 开发规划：`docs/重构开发规划-V4.md`、`项目开发规划.md`（V2）
- 项目反思：`docs/项目反思.md`
- 自动化测试：`test/v4-pipeline.mjs`、`test/smoke-v4.mjs`、`test/run-engine.mjs`（V2 规则引擎 6 样例断言）
- 数据库脚本：`db/schema.sql`（V2）、`db/schema-v4.sql`（V4）；`npm run db:setup` 一键建表
- 环境变量：`.env.local`（本地，不入库）；Vercel 侧同名配置 `DATABASE_URL`、`DEEPSEEK_API_KEY` 等

## 红线自查
- 已部署可访问在线系统（第 1 项）✅
- 已提供 20,000 SKU 灌入脚本（第 3 项）✅
- 已提供 10,000 行压测 Excel（第 4 项）✅
- 主链路为异步事件驱动，非「同步逐行 INSERT」✅
- 复用 V2 规则引擎，未为压测文件写死解析逻辑 ✅
- 代码/仓库不含真实密钥（.env.local 已 gitignore；Vercel 用环境变量）✅
